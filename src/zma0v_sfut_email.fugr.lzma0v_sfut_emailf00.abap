*---------------------------------------------------------------------*
*    view related FORM routines
*   generation date: 01/29/2021 at 11:00:10
*   view maintenance generator version: #001407#
*---------------------------------------------------------------------*
*...processing: ZMA0V_SFUT_EMAIL................................*
FORM GET_DATA_ZMA0V_SFUT_EMAIL.
  PERFORM VIM_FILL_WHERETAB.
*.read data from database.............................................*
  REFRESH TOTAL.
  CLEAR   TOTAL.
  SELECT * FROM ZMA0_SFUT_EMAIL WHERE
(VIM_WHERETAB) .
    CLEAR ZMA0V_SFUT_EMAIL .
ZMA0V_SFUT_EMAIL-MANDT =
ZMA0_SFUT_EMAIL-MANDT .
ZMA0V_SFUT_EMAIL-EKORG =
ZMA0_SFUT_EMAIL-EKORG .
ZMA0V_SFUT_EMAIL-SUBJECT =
ZMA0_SFUT_EMAIL-SUBJECT .
    SELECT SINGLE * FROM T024E WHERE
EKORG = ZMA0_SFUT_EMAIL-EKORG .
    IF SY-SUBRC EQ 0.
ZMA0V_SFUT_EMAIL-EKOTX =
T024E-EKOTX .
    ENDIF.
<VIM_TOTAL_STRUC> = ZMA0V_SFUT_EMAIL.
    APPEND TOTAL.
  ENDSELECT.
  SORT TOTAL BY <VIM_XTOTAL_KEY>.
  <STATUS>-ALR_SORTED = 'R'.
*.check dynamic selectoptions (not in DDIC)...........................*
  IF X_HEADER-SELECTION NE SPACE.
    PERFORM CHECK_DYNAMIC_SELECT_OPTIONS.
  ELSEIF X_HEADER-DELMDTFLAG NE SPACE.
    PERFORM BUILD_MAINKEY_TAB.
  ENDIF.
  REFRESH EXTRACT.
ENDFORM.
*---------------------------------------------------------------------*
FORM DB_UPD_ZMA0V_SFUT_EMAIL .
*.process data base updates/inserts/deletes.........................*
LOOP AT TOTAL.
  CHECK <ACTION> NE ORIGINAL.
MOVE <VIM_TOTAL_STRUC> TO ZMA0V_SFUT_EMAIL.
  IF <ACTION> = UPDATE_GELOESCHT.
    <ACTION> = GELOESCHT.
  ENDIF.
  CASE <ACTION>.
   WHEN NEUER_GELOESCHT.
IF STATUS_ZMA0V_SFUT_EMAIL-ST_DELETE EQ GELOESCHT.
     READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY>.
     IF SY-SUBRC EQ 0.
       DELETE EXTRACT INDEX SY-TABIX.
     ENDIF.
    ENDIF.
    DELETE TOTAL.
    IF X_HEADER-DELMDTFLAG NE SPACE.
      PERFORM DELETE_FROM_MAINKEY_TAB.
    ENDIF.
   WHEN GELOESCHT.
  SELECT SINGLE FOR UPDATE * FROM ZMA0_SFUT_EMAIL WHERE
  EKORG = ZMA0V_SFUT_EMAIL-EKORG .
    IF SY-SUBRC = 0.
    DELETE ZMA0_SFUT_EMAIL .
    ENDIF.
    IF STATUS-DELETE EQ GELOESCHT.
      READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY> BINARY SEARCH.
      DELETE EXTRACT INDEX SY-TABIX.
    ENDIF.
    DELETE TOTAL.
    IF X_HEADER-DELMDTFLAG NE SPACE.
      PERFORM DELETE_FROM_MAINKEY_TAB.
    ENDIF.
   WHEN OTHERS.
  SELECT SINGLE FOR UPDATE * FROM ZMA0_SFUT_EMAIL WHERE
  EKORG = ZMA0V_SFUT_EMAIL-EKORG .
    IF SY-SUBRC <> 0.   "insert preprocessing: init WA
      CLEAR ZMA0_SFUT_EMAIL.
    ENDIF.
ZMA0_SFUT_EMAIL-MANDT =
ZMA0V_SFUT_EMAIL-MANDT .
ZMA0_SFUT_EMAIL-EKORG =
ZMA0V_SFUT_EMAIL-EKORG .
ZMA0_SFUT_EMAIL-SUBJECT =
ZMA0V_SFUT_EMAIL-SUBJECT .
    IF SY-SUBRC = 0.
    UPDATE ZMA0_SFUT_EMAIL .
    ELSE.
    INSERT ZMA0_SFUT_EMAIL .
    ENDIF.
    READ TABLE EXTRACT WITH KEY <VIM_XTOTAL_KEY>.
    IF SY-SUBRC EQ 0.
      <XACT> = ORIGINAL.
      MODIFY EXTRACT INDEX SY-TABIX.
    ENDIF.
    <ACTION> = ORIGINAL.
    MODIFY TOTAL.
  ENDCASE.
ENDLOOP.
CLEAR: STATUS_ZMA0V_SFUT_EMAIL-UPD_FLAG,
STATUS_ZMA0V_SFUT_EMAIL-UPD_CHECKD.
MESSAGE S018(SV).
ENDFORM.
*---------------------------------------------------------------------*
FORM READ_SINGLE_ZMA0V_SFUT_EMAIL.
  SELECT SINGLE * FROM ZMA0_SFUT_EMAIL WHERE
EKORG = ZMA0V_SFUT_EMAIL-EKORG .
ZMA0V_SFUT_EMAIL-MANDT =
ZMA0_SFUT_EMAIL-MANDT .
ZMA0V_SFUT_EMAIL-EKORG =
ZMA0_SFUT_EMAIL-EKORG .
ZMA0V_SFUT_EMAIL-SUBJECT =
ZMA0_SFUT_EMAIL-SUBJECT .
    SELECT SINGLE * FROM T024E WHERE
EKORG = ZMA0_SFUT_EMAIL-EKORG .
    IF SY-SUBRC EQ 0.
ZMA0V_SFUT_EMAIL-EKOTX =
T024E-EKOTX .
    ELSE.
      CLEAR SY-SUBRC.
      CLEAR ZMA0V_SFUT_EMAIL-EKOTX .
    ENDIF.
ENDFORM.
*---------------------------------------------------------------------*
FORM CORR_MAINT_ZMA0V_SFUT_EMAIL USING VALUE(CM_ACTION) RC.
  DATA: RETCODE LIKE SY-SUBRC, COUNT TYPE I, TRSP_KEYLEN TYPE SYFLENG.
  FIELD-SYMBOLS: <TAB_KEY_X> TYPE X.
  CLEAR RC.
MOVE ZMA0V_SFUT_EMAIL-EKORG TO
ZMA0_SFUT_EMAIL-EKORG .
MOVE ZMA0V_SFUT_EMAIL-MANDT TO
ZMA0_SFUT_EMAIL-MANDT .
  CORR_KEYTAB             =  E071K.
  CORR_KEYTAB-OBJNAME     = 'ZMA0_SFUT_EMAIL'.
  IF NOT <vim_corr_keyx> IS ASSIGNED.
    ASSIGN CORR_KEYTAB-TABKEY TO <vim_corr_keyx> CASTING.
  ENDIF.
  ASSIGN ZMA0_SFUT_EMAIL TO <TAB_KEY_X> CASTING.
  PERFORM VIM_GET_TRSPKEYLEN
    USING 'ZMA0_SFUT_EMAIL'
    CHANGING TRSP_KEYLEN.
  <VIM_CORR_KEYX>(TRSP_KEYLEN) = <TAB_KEY_X>(TRSP_KEYLEN).
  PERFORM UPDATE_CORR_KEYTAB USING CM_ACTION RETCODE.
  ADD: RETCODE TO RC, 1 TO COUNT.
  IF RC LT COUNT AND CM_ACTION NE PRUEFEN.
    CLEAR RC.
  ENDIF.

ENDFORM.
*---------------------------------------------------------------------*
FORM COMPL_ZMA0V_SFUT_EMAIL USING WORKAREA.
*      provides (read-only) fields from secondary tables related
*      to primary tables by foreignkey relationships
ZMA0_SFUT_EMAIL-MANDT =
ZMA0V_SFUT_EMAIL-MANDT .
ZMA0_SFUT_EMAIL-EKORG =
ZMA0V_SFUT_EMAIL-EKORG .
ZMA0_SFUT_EMAIL-SUBJECT =
ZMA0V_SFUT_EMAIL-SUBJECT .
    SELECT SINGLE * FROM T024E WHERE
EKORG = ZMA0_SFUT_EMAIL-EKORG .
    IF SY-SUBRC EQ 0.
ZMA0V_SFUT_EMAIL-EKOTX =
T024E-EKOTX .
    ELSE.
      CLEAR SY-SUBRC.
      CLEAR ZMA0V_SFUT_EMAIL-EKOTX .
    ENDIF.
ENDFORM.
